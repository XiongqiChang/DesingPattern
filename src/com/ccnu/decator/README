装饰者模式
   允许向一个已经存在的对象添加一个新的功能，并且同时又不改变其结构，这个就是动态的向对象添加一个新的功能，装饰器模式比子类更加的
   灵活。是一种结构性模式的，提供了比继承更加好弹性功能。但是在同时会出现很多的类的出现，会增加程序的复杂性
   1:首先定义的是一个抽象的类，定义的是一个抽象的煎饼类，里面具有的是两个抽象的方法，一个是描述还有一个是价钱的
   2：声明一个实体的加煎饼，实现里面的方法
   3：定义一个装饰者继承抽象的煎饼，然后里面定义一个实体的煎饼
   4：然后我们具体的实现类的话就是继承装饰者方法的。