观察者模式：
    定义对象之间的一对多之间的依赖，指的是一个对象的状态发生了改变，那么这个对象就是会去头通知到其他的对象，
    然后通知进行状态的改变的。
    比如是在Zookeeper中的话，就是运用的是一种观察者的模式的。
    三个角色
        1：一个是Subject的，还有一个是Observer的，还有一个是Client的，这就是说我吗需要在Subject中需要注册到
        一系列的Observer的